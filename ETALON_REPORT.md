# Эталоны — что удалось померить

В проекте теперь два размеченных набора, и они меряют **разное**. Оба нужны.

| набор | заявок | что меряет |
|---|---|---|
| `data/etalon_469.jsonl` ← `AVTOZAP_ETALON_GOTOVYY.csv` | 469 | **точность**: у каждой заявки есть правильный код |
| `data/etalon_200.jsonl` ← `AVTOZAP_etalon_200.xlsx` | 200 | **поведение**: 27 заявок, где верный ответ — отказ или переспрос, плюс точка отсчёта 128 |

Пересобрать оба: `python scripts/build_etalon_input.py`.

## Почему старый набор нельзя выбрасывать

Новый файл пересекается со старым всего по **27 текстам из 469**, то есть это
не исправленная версия, а другая выборка. И главное:

* в новом наборе **нет ни одной заявки с пустым правильным кодом** — из тех 27
  «отказных» строк старого набора в новый не попала ни одна;
* в новом наборе **нет колонки с ответом боевой системы**, то есть точки
  отсчёта «128 из 200» в нём тоже нет.

Правило «не уверен — переспроси, не знаешь названия — проси фото» осталось без
изменений, как вы и сказали. Но проверить его можно только на наборе из 200:
если считать лишь по 469, система, которая никогда не отказывается, выглядит
безупречной по этой оси. Поэтому оба набора остались, и оба покрыты тестами.

Единственное расхождение по общим текстам — одно, и оно осмысленное:
`on podpresin surucu teref kanti` был KZ-038 (общий «хром-кант»), стал KZ-095
(«кант лобового стекла»). Разметка стала точнее.

## Словарь 571 — все 469 строк считаются

Выгрузка от 01.09 закрыла вопрос: KZ-094 и KZ-095 на месте, строк вне подсчёта
больше нет. Механизм `scorable` остался — если разметка снова обгонит словарь,
такие строки не будут ни засчитаны, ни провалены.

Вторая поставка (`AVTOZAP_dlya_matchera_1.zip`) добавила словарь
`AVTOZAP_slovar_FINAL_31avg_v4.xlsx`, термины `AVTOZAP_terminy_v5.csv` и новый
`avtozap_ambiguity_layer.py`. Деталей по-прежнему 571, терминов стало 4651
против 4383. Контрольный прогон — **500 / 69 / 135**, ровно как в вашем
changelog.

### Поставка v8 — проверена по файлу, все заявленное подтвердилось

Заявлено семь пунктов, проверил каждый механически, а не по описанию:

| заявлено | проверка |
|---|---|
| MU-012 деактивирован → MU-078 | ✔ `is_active = No`, 5 форм перенесено |
| EL-013 деактивирован → AK-028 | ✔ `is_active = No`, 9 форм перенесено |
| AS-029 и AS-034 раздельно, `çaşka` в AMBIGUOUS_RULES | ✔ оба активны, правило пятое |
| формы для KZ-016, KZ-050, AK-020, SO-009, SU-002, MU-038, AS-002 | ✔ все 18 форм на месте |

Отдельно сверил, **не изменилось ли что-то незаявленное**: добавлен 41 термин,
убрано 12, и все 12 — у двух деактивированных кодов. Ни одна другая деталь не
потеряла ни одной формы. Изменений в `parts_synonyms` — ровно 13 деталей, все
из списка выше. Чисто.

`data/duplicate_codes.json` поехал вместе со словарём: добавлена пара
EL-013 → AK-028. Появился тест, который теперь этого требует: деактивированный
код обязан иметь каноническую замену, а замена — вести на живой код.

### Результат v8

Контрольный прогон **500 / 68 / 136**, единица вернулась. Изменилась ровно одна
строка из 704: `aktivatur turbnun ustundə olan` стала находить MU-038.

| | v7 | **v8** |
|---|---|---|
| 469: код в shortlist | 467 | **469 = 100%** |
| 469: код на первом месте | 411 | 410 |
| 200: код в shortlist | 164 | **170** |
| 200: код на первом месте | 123 | **129** |
| потолок 469 | 467/469 | **469/469 = 100%** |
| потолок 200 | 191/200 | **197/200 = 98.5%** |
| запас над боевыми 128 | +63 | **+69** |

**На эталоне 469 потолок закрыт полностью.** Промахов ретривера не осталось ни
одного. На эталоне 200 их три, и все три ретривером не решаются в принципе:

| заявка | почему нерешаемо |
|---|---|
| `Zapchastin kodu: 25819 0w010` | ответ в каталожном номере — нужен каталог OEM |
| `Kia k5 … amerikanka 3 lupa cut` | сленг, которого нет в словаре |
| `Bu firmaynan var?` | ответ в предыдущем сообщении переписки |

### Построчный разбор: 8 приобретений, 0 потерь, 2 перестановки

Сверил каждую из 642 оцениваемых строк обоих эталонов до и после.

**Появились в shortlist — 8**, пропали — **0**. Семь из восьми сразу встали на
первое место: KZ-016, KZ-050, AK-020, SO-009, SU-002, MU-038 и AS-002 — ровно
те коды, для которых вы добавляли формы. AS-029 (`Qabaq sağ sol çaşka
lenforderle`) в список попал, но на первое место не встал.

**Упали с первого места — 2**, и обе стоит разобрать.

**`Park radar - 96890-A5000YAC`** — следствие слияния EL-013 → AK-028. Раньше
EL-013 стоял первым, теперь первым идёт EL-014 «Блок парктроника», а нужный
AK-028 вторым. Заявка с каталожным номером: с каталогом OEM решалась бы
однозначно, без него выбор между блоком и датчиками сделать не на чем.

**`Krlonun üstündə olan benzin qapağı ağ rəng`** — вот это находка, и она не
про эту строку.

### Одна строка словаря, которая роняет score чужим деталям

KZ-083 «Лючок топливного бака» упал с 0.9999 до 0.88 и пропустил вперёд EL-021
«Актуатор багажника/лючка бензобака». При этом **KZ-083 в словаре не менялся
вообще** — ни термина, ни синонима.

Причина — форма `aktivatur turbnun ustunde olan`, добавленная к MU-038. Это не
термин, а целое предложение, и вместе с ним в индекс попали служебные слова
`üstündə` и `olan`. После этого они начинают «матчить» любую заявку, где эти
слова есть, и ровняют score деталям, которые к турбине отношения не имеют.
Видно прямо в причинах: у KZ-083 строка причины **не изменилась**, а score
упал — то есть дело не в нём.

Проверил экспериментом: убрал из словаря **одну эту строку**, ничего больше.

| | KZ-083 | MU-038 |
|---|---|---|
| со строкой | 0.88, второе место | найден, первое место |
| без строки | **0.9999, первое место** | не найден вовсе |

По замеру на 642 строках обоих эталонов строка **ровно нулевая**: даёт первое
место `etalon-023`, отнимает у `etalon469-265`. Shortlist не двигает никак.

Поэтому это не «уберите её», а «есть способ получить и то и другое». Две другие
формы (`turbo aktivaturu`, `turbonun aktivaturu`) живой текст
`aktivatur turbnun ustundə olan` не покрывают. Покрыло бы **контекстное
правило** `aktivatur + turb` → MU-038 — тем же механизмом, которым вы уже
разводите `qapi + kilid motoru`. Правило сочетание слов проверяет, а служебные
слова в индекс не тащит. Решение ваше, в словарь я ничего не вношу.

И общий вывод из этого случая: **длинные фразы со служебными словами лучше
заводить контекстными правилами, а не терминами.** Здесь это стоило одной
строки, но механизм действует на весь словарь сразу.

## Поставка v7

Третья поставка — `AVTOZAP_slovar_FINAL_v7.xlsx`, установлена. Это чистка, а не
рост: 90 терминов убрано, 62 добавлено, затронуто 28 деталей — ровно то, что
описано в листах `CLEANUP_v6` и `FINAL_v7`. Появились два новых листа, и оба
пришлось учесть в коде (см. ниже). Контрольный прогон — **499 / 68 / 137**.

Число снизилось на единицу, и это **не регресс**. Я сверил прогон построчно:
изменились ровно три строки из 704.

| строка | было | стало | что это |
|---|---|---|---|
| `Sağ tərəf almacıq` | SU-002, неверно | **SU-003, верно** | ваша правка `MOVE SU-002 → SU-003` |
| `Arxa şveller` | KZ-030, верно | не найдено | термин `sveler` объявлен неоднозначным |
| `Arxa Şveller Hyundai Elantra 2019-2020` | KZ-030, верно | не найдено | то же |

То есть минус два «верно» — это ваше собственное решение не угадывать между
порогом и усилителем бампера. В счётчике матчера такая заявка честно уходит в
«не найдено», а в нашей цепочке она уходит в уточняющий вопрос — и это, на мой
взгляд, ровно то поведение, которого вы и добивались.

## Дубли кодов — четыре строки поправлены на импорте

В новой разметке снова встретились коды, которые вы объявили дублями:

| в разметке | должно быть | заявка |
|---|---|---|
| SO-025 | SO-005 | `Termostat korpusu zbor` |
| MU-012 | MU-078 | `1.5 eco blu mator qalofka praklatkası` (и ещё одна) |
| RG-04 | MU-089 | `hava xortumu` |

Приводятся к каноническим автоматически при сборке входа, тестом закреплено,
что после этого ни одна строка на дубль не ссылается.

## Потолок цепочки

Считается без API. Арбитр выбирает только из того, что дал ретривер, — выше
этого подняться нельзя физически.

| | набор 469 | набор 200 |
|---|---|---|
| заявок с ожидаемым кодом | 469 | 173 |
| код в shortlist по сырому тексту | 468 = 99.8% | 166 = 96.0% |
| **код в shortlist после Layer 0** | **469 = 100%** | **170 = 98.3%** |
| из них на первом месте | 410 = 87.4% | 129 = 74.6% |
| потолок всей цепочки | **469/469 = 100%** | **197/200 = 98.5%** |

Как это росло:

| поставка словаря | 469: shortlist / rank#1 | 200: shortlist / rank#1 | промахов |
|---|---|---|---|
| 541 деталь | 90.3% / — | 90.5% / — | 45 и 19 |
| 571, выгрузка 01.09 | 465 / 334 | 160 / 114 | 4 и 13 |
| 571, вторая поставка | 467 / 406 | 164 / 122 | 2 и 9 |
| v7 | 467 / 411 | 164 / 123 | 2 и 9 |
| **v8** | **469 / 410** | **170 / 129** | **0 и 3** |

Две фазы видны невооружённым глазом. С 541 по v7 shortlist почти не двигался, а
rank#1 рос с 334 до 411 — чистки **разводили похожее**. В v8 добавились
недостающие формы, и сдвинулся уже сам shortlist: на 469 он закрыт полностью.
На наборе 200 запас над боевыми 128 — **плюс 69 заявок**.

Практический вывод: на наборе 469 словарь исчерпан — потолок 100%, поднимать
больше нечего. Всё, что осталось между 100% и реальным результатом, — это
ошибки арбитра, и увидеть их можно только живым прогоном.

Разница между потолком и результатом прогона — цена ошибок арбитра. Ровно её и
покажет живой прогон.

## Цифры прогона по-прежнему нет

`OPENAI_API_KEY` в моём окружении нет, прогнать цепочку по API я не могу.
Mock-число подставлять не буду: заглушка арбитра осторожна по построению и
описывает саму себя, а не цепочку.

```bash
python run_test.py --input data/etalon_469.jsonl
python scripts/score_etalon.py --results out/results.jsonl

# и отдельно — проверка правила отказов и переспроса
python run_test.py --input data/etalon_200.jsonl
python scripts/score_etalon.py --etalon data/etalon_200.jsonl --results out/results.jsonl
```

Второй командой печатается доля попаданий по каждому уровню уверенности
арбитра — по ней и ставится порог.

## Правило переспроса и фото — без изменений

Как договорились, `avtozap/policy.py` не трогал:

| действие | когда |
|---|---|
| `ANSWER` | деталь определена |
| `ASK_BUYER` | не уверены — просим описать другими словами |
| `ASK_PHOTO` | покупатель сам пишет, что не знает названия |
| `PASS_TO_SHOP` | второй заход (`--attempt 2`) не помог — сырой текст магазинам |

Просьба о фото по-прежнему имеет приоритет над «отдать магазинам».

## Два согласованных правила — сделаны

**Вязкость вместо марки** (`avtozap/lubricants.py`). `5w-40`, `0/20`, `75W90`,
`DOT-4` — это формат, а не словарь, и по нему видно, что речь о масле или
тормозной жидкости, какая бы марка ни стояла рядом. Обе заявки на масло из
списка промахов закрылись. Трансмиссионное отличается от моторного: рядом со
словом `karopka` вязкость читается как трансмиссионная. Размер шины
`205/55R16` под правило не попадает — проверено.

**Ранжирование по головному слову** (`avtozap/head_ranking.py`). Отдельный
проход поверх вендорного ретривера, его код не тронут — контрольные 499/68/137
остаются теми же по построению. Головой считаем последнее слово, **известное
словарю**: в `Qabaq sağ sol çaşka lenforderle` хвостовая марка головой не
становится.

**Перемеряю его на каждой поставке**, потому что вопрос честный: не перекрыли
ли правило ваши контекстные правила. Не перекрыли, и с каждой поставкой оно
работает чуть лучше:

| | без правила | с правилом |
|---|---|---|
| 469, код на первом месте | 400 | **411** (+11) |
| 200, код на первом месте | 120 | **123** (+3) |

Набор 200 в подборе правила не участвовал, и там прирост тоже есть — значит это
не подгонка. Динамика по поставкам: было +11 и 0, потом +12 и +3, теперь +11
и +3. Состав shortlist не меняется ни на одном наборе: потолок правило не
двигает, только порядок внутри списка.

Вывод: ваши контекстные правила закрывают конкретные сочетания слов, а это
правило — общую грамматику «владелец/деталь». Пересекаются они мало.

Надбавку тоже перепроверяю: 0.005 и 0.01 дают одно и то же, 0.02 и 0.04 хуже.
Оставлено 0.01. Штраф кандидату без головного слова по-прежнему вреден и не
включён.

## Неоднозначные термины v7 — исполнены, но не так, как задумано

В v7 появился лист `AMBIGUOUS_RULES`: четыре термина, по которым вы запретили
выбирать код без уточняющего контекста, с готовым вопросом покупателю к
каждому. Я это внедрил — и по дороге нашёл, что **одного снятия алиасов
недостаточно**.

Вы убрали неоднозначные термины из уверенных алиасов. Движок после этого не
замолчал:

| голый запрос | правило говорит | движок отдаёт |
|---|---|---|
| `park radari` | EL-014 или AK-028, уточнить | **EL-013** score 1.0 |
| `teker sensoru` | EY-011 или TK-005, уточнить | TK-005 score 1.0 (`context:teker+sensor`) |
| `baqaj jaluzu` | TY-016 или KZ-070, уточнить | KZ-070 score 1.0 (`context:baqaj+jaluz`) |
| `Arxa şveller` | KZ-030 или KZ-043, уточнить | **KZ-012**, стекло багажника |

Уверенность держится на основах, нечётком сравнении и ваших же контекстных
правилах — они срабатывают и без алиасов. То есть арбитр видел уверенного
кандидата ровно там, где вы уверенности запретили, а в двух случаях — код,
которого нет ни в одной из пар. Это закреплено тестом: если словарь однажды
научится молчать сам, тест упадёт и слой можно будет упростить.

**Что сделано.** `scripts/extract_ambiguous_rules.py` переносит лист в
`data/ambiguous_rules.json` (руками ничего не переписано, тест сверяет файл с
листом до буквы), `avtozap/ambiguity.py` исполняет правило, конвейер на таком
запросе не отвечает и задаёт **ваш** вопрос.

**Правило заказчика не менялось** — как договаривались. Действие остаётся
`ASK_BUYER`, меняется только текст: вместо общего «опишите другими словами»
покупатель получает вопрос, на который отвечают одним словом.

**Голым** термин считается, если кроме него в запросе нет ничего, кроме слов
положения и вежливости. Дописал значащее слово — правило молчит:

| запрос | что происходит |
|---|---|
| `Arxa şveller` | вопрос: порог или усилитель бампера? |
| `porog sveleri` | ответ KZ-030 — отвечает ваш словарь |
| `park radari` | вопрос: блок или датчик? |
| `park radari bloku` | ответ EL-014 — отвечает ваш словарь |

Так вопрос не может отобрать ответ у того, кто написал понятно. Проверено на
всех четырёх терминах.

### Чего это стоит и один вопрос к вам

На эталоне 200 правило не срабатывает ни разу. На эталоне 469 — **один раз**,
и этот случай стоит разобрать:

`Arxa park radari`, ожидается **AK-028** «Парковочные датчики», помечено
«подтверждено вручную». Наш слой на нём переспросит вместо ответа. Но
посмотрите, что даёт ретривер без слоя **после слияния EL-013 в AK-028**:

| место | код | название |
|---|---|---|
| 1 | **EL-014** | Блок парктроника |
| 2 | AK-028 | Парковочные датчики |

Первым по-прежнему идёт не то, что просили. То есть без слоя заявка была бы
отвечена **неверно**, а со слоем покупатель получает ровно тот вопрос, который
здесь и нужен: «блок или датчик?». Слияние EL-013 → AK-028 картину не
изменило — выбор между блоком и датчиками остаётся, и разрешить его может
только покупатель.

Оба вопроса по словарю, которые я задавал, **закрыты в v8**: EL-013 сведён с
AK-028, MU-012 — с MU-078, оба деактивированы, `duplicate_codes.json`
обновлён.

## Что осталось в ретривере — ноль на 469, три на 200

Было 45, потом 4, потом 2, теперь **ноль**. Обе оставшиеся заявки закрыла v8:

| заявка | нужен | как закрылось |
|---|---|---|
| `arxa baqaj qapisina rucka lazımdır` | KZ-016 | формы `baqaj qapisina rucka`, сразу первое место |
| `Qabaq sağ sol çaşka lenforderle` | AS-029 | формы `alt çaşka` / `purjun alt çaşkası`, в shortlist |

На эталоне 200 осталось три, и ни одна из них ретривером не решается:

| заявка | нужен | почему |
|---|---|---|
| `Zapchastin kodu: 25819 0w010` | MU-050 | ответ в каталожном номере — **нужен каталог OEM** |
| `Kia k5 … amerikanka 3 lupa cut` | KZ-005 | сленг, которого в словаре нет |
| `Bu firmaynan var?` | MU-055 | ответ в предыдущем сообщении переписки |

Первая закрывается каталогом OEM, вторая — словарём, если решите заводить
сленг, третья — только если в выгрузку попадёт предыдущее сообщение.

Полный список — `out/retriever_misses_etalon.csv`.

## Что дальше

1. **Прогнать `python3 PROGON_ETALON_469.py` с ключом.** Теперь это не просто
   «осталось несделанным» — это единственное, что вообще осталось. Потолок на
   469 закрыт полностью, всё расстояние между 100% и результатом — это арбитр,
   и другого способа его увидеть нет. Ключ мне присылать не нужно: запустите
   сами и пришлите папку `out/etalon469/`, в ней ключа нет.
2. По таблице уверенности поставить порог.
3. **Каталог OEM**, 40 соответствий «номер → деталь». Из трёх оставшихся
   промахов на 200 он закрывает один и разрешает `Park radar - 96890-A5000YAC`,
   где выбор между блоком и датчиками иначе сделать не на чем. Формат ждёт в
   `data/oem_catalog.json`.
4. Решить по строке `aktivatur turbnun ustunde olan`: оставить термином или
   перевести в контекстное правило `aktivatur + turb` (разбор выше).
5. Два оставшихся промаха на 200: сленг `amerikanka` / `lupa` → KZ-005, и есть
   ли в выгрузке предыдущее сообщение переписки.
