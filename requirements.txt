# Единственная внешняя зависимость конвейера — клиент OpenAI (Arbiter V3 и слой фото).
# Все детерминированные слои (Layer 0, OEM, Retriever V2, валидатор) работают
# на стандартной библиотеке, поэтому --mock и --dry-run запускаются без сети.
openai>=1.40,<4

# только для тестов
pytest>=7.4
